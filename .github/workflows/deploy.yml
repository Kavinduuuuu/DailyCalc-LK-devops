name: FTP Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Deploy via FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ftp.infinityfree.com
        username: if0_40983186
        password: Shakya0501  # ← SECURE!
        local-dir: ./
        server-dir: /htdocs/